function solucao(numero1, numero2) {
    const resto = numero1 % numero2; // o resto sempre será o que sobra do número 1 dividido pelo número 2
    const resposta = resto === 0; // quando qualquer números  acima forem divididos e seu resto for 0, ele é divisível;
}